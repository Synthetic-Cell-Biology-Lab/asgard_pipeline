# ==========================================================
# Folder Metadata - Templates
# ==========================================================
expand_subfolders: true
enforce_file_tracking: true

description: >
  Templates used to standardize process and configuration metadata files.

content_type: templates
curation_status: active
owner: Anirudh
version_tag: 0.1.2

# ----------------------------------------------------------
# FILE-LEVEL DOCUMENTATION
# ----------------------------------------------------------

files:
  folder_metadata.yaml: >
    Metadata index for the templates directory.

  folder_metadata.txt: >
    Reference template demonstrating the expected schema and fields
    for folder_metadata.yaml files.

# ----------------------------------------------------------
# APPENDIX SECTION (Printed in Report)
# ----------------------------------------------------------

appendix:
  Coverage: >
    Includes all files currently present in templates/.
